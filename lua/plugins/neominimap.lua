return {
  "Isrothy/neominimap.nvim",
  version = "v3.x.x",
  priority = 100, -- Set a higher priority to load before neo-tree
  lazy = false,
  keys = {},
  click = {
    enable = true,
    auto_switch_focus = true,
  },
  -- Remove opts to see if plugin loads minimally
}
